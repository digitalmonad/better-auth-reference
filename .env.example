DATABASE_URL=""
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="" # e.g. http://localhost:3000

# Email Configuration
EMAIL_FROM=""

# Option 1: Use local Mailpit (Docker) - recommended for development
SMTP_HOST="localhost"  # or "mailpit" if running in Docker network
SMTP_PORT="1025"

# Option 2: Use Mailtrap cloud (comment out SMTP_HOST to use this)
# SMTP_HOST="sandbox.smtp.mailtrap.io"
# SMTP_PORT="2525"
MAILTRAP_USERNAME=""
MAILTRAP_PASSWORD=""

AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""
